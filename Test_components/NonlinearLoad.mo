within HPF.Test_components;

model NonlinearLoad
extends Interfaces.TwoPinBase;

equation

annotation(
    Icon(coordinateSystem(grid = {0, 0})),
    Diagram(coordinateSystem(extent = {{-200, -200}, {200, 200}}, grid = {0, 0})));end NonlinearLoad;